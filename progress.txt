## Codebase Patterns
- Room JVM unit tests (Robolectric): add `testImplementation("org.robolectric:robolectric:4.11.1")`, `androidx.test:core:1.5.0`, `kotlinx-coroutines-test`; set `@Config(sdk = [34])` to avoid compileSdk=35 warnings; enable `testOptions { unitTests.isIncludeAndroidResources = true }` in the Android block
- Use `Room.inMemoryDatabaseBuilder(...).allowMainThreadQueries()` for DAO tests in `src/test/`; collect flows with `.first()` inside `runTest {}`
- `GymAppRoot.kt` is the single root composable wrapping NavHost + Scaffold — global UI overlays (chat, notifications) belong here, outside NavHost
- `ModalBottomSheet` with `skipPartiallyExpanded = true` used for overlay sheet — placed after Scaffold in composition, not inside it
- Use `Icons.AutoMirrored.Filled.*` for Chat and Send icons (Filled variants deprecated in recent Material Icons)
- Feature overlays that should persist across navigation live in `app/` module, not in feature modules
- `ChatViewModel` lives in `app/` module (not a feature module) because it drives a global overlay
- `hiltViewModel()` called in `GymAppRoot` scope — ViewModel survives navigation because it's at the root composable level
- `LazyColumn` with `reverseLayout = true` + `items(messages.reversed())` pattern for chat lists — newest message at bottom; loading indicator inserted as first `item {}` block in reversed order
- `mutableStateListOf<T>` for ViewModel-owned lists that drive Compose recomposition without StateFlow overhead

---

## 2026-03-14T23:00 - TC-002
- Added Robolectric + room-testing + coroutines-test deps to `core/core-database/build.gradle.kts`
- Created `core/core-database/src/test/kotlin/com/gymapp/core/database/MovementHistoryTest.kt` (12 tests)
- Tests cover: empty history, single-session, multi-set best-weight/reps, date ordering (DESC), exercise isolation, getPreviousSessionSets, getPreviousSessionDate, observeProgressSummaries
- All edge cases: no sessions, unknown exercise, single session, sessions with multiple sets
- Files changed: `core/core-database/build.gradle.kts`, `core/core-database/src/test/kotlin/com/gymapp/core/database/MovementHistoryTest.kt`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Robolectric 4.11.1 + `@Config(sdk = [34])` works cleanly for JVM Room tests; avoids compileSdk=35 warning
  - `allowMainThreadQueries()` needed for Flow `.first()` in JVM tests (no coroutine dispatcher set up)
  - Test fixtures: simple private suspend helpers (session/exercise/set) keep test bodies clean
---

## 2026-03-14T22:30 - US-002
- Implemented `ChatViewModel` (app module) with `AiAssistant` integration, in-memory message list, loading state, and error handling
- Updated `GymAppRoot.kt`: `ChatOverlay` now accepts ViewModel state; message bubbles with user (right, primaryContainer) / AI (left, surfaceVariant) / error (errorContainer) styling; `CircularProgressIndicator` loading item; send button disabled while loading
- Added `core:core-ai` and `lifecycle-viewmodel-compose` deps to `app/build.gradle.kts`
- System prompt: personal training coach persona — concise, direct, no hedging
- Files changed: `app/build.gradle.kts`, `app/src/main/kotlin/com/gymapp/ChatViewModel.kt` (new), `app/src/main/kotlin/com/gymapp/GymAppRoot.kt`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - `AiAssistant.chat()` already handles API key reads from DataStore — just pass system prompt + message history
  - Error text from `AiAssistant` is surfaced via `Result.failure` — catch and show inline, never crash
  - Input cleared after send by resetting `inputText = ""` in the `onClick` lambda (not in ViewModel)
---

## 2026-03-14T22:00 - US-001
- Implemented floating chat FAB and dismissible overlay shell in `GymAppRoot.kt`
- FAB uses `Icons.AutoMirrored.Filled.Chat`, hidden when overlay is open
- Overlay is a `ModalBottomSheet` with `skipPartiallyExpanded = true`, containing: title row + close (X) button, empty `LazyColumn` for messages, and `OutlinedTextField` + send `IconButton`
- All state is local (`remember { mutableStateOf(...) }`) — no ViewModel needed for scaffolding
- Files changed: `app/src/main/kotlin/com/gymapp/GymAppRoot.kt`, `prd.json`
- **Learnings for future iterations:**
  - `ModalBottomSheet` must be placed outside `Scaffold` but inside `GymAppTheme` to overlay correctly
  - `dragHandle = null` removes the default handle; use explicit close button instead for cleaner UX
  - `imePadding()` on the Column inside the sheet handles keyboard correctly
  - New branch `ralph/ai-assistant` created from `main`
---
